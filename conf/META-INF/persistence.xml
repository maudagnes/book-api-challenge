<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <!-- 
        JPA Persistence Unit Configuration
        Defines how Hibernate connects to the database and manages entities
    -->
    <persistence-unit name="defaultPersistenceUnit" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <non-jta-data-source>DefaultDS</non-jta-data-source>
        <!-- Register the Book entity class -->
        <class>models.Book</class>    
        <properties>
            <!-- H2 SQL dialect for query generation -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>            
            <!-- Auto-generate/update database schema from @Entity classes -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- Show SQL statements in the console -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

</persistence>